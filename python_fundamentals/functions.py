#Production fn- Y = F(K,L)

#Cobb-Douglas production fn: Y = zK**αL**1-α

#z = total factor prductivity ( TFP)


#PYTHON FNS

def mean(numbers):
    total = sum(numbers)
    N = len(numbers)
    answer = total / N
    return answer


x = [1, 2, 3, 4]

the_mean = mean(x)
print(the_mean)
